from __future__ import annotations
from app.utils.config import settings

# Placeholder for webhook setup if needed in the future.
# PTB webhook setup could be added here using Application.run_webhook
WEBHOOK_URL = None  # set to actual URL if deploying webhook mode
